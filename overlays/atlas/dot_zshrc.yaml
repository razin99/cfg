plugins:
  - aws
  - direnv
  - terraform
aliases:
  k: kubectl
  pinentry: pinentry-mac
  lzd: lazydocker
paths:
  - $HOME/.local/bin
  - $HOME/.cargo/bin
  - ${KREW_ROOT:-$HOME/.krew}/bin
  - /opt/homebrew/bin/curl/bin
